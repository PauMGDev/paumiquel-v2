Read ROADMAP.md. Identify the first unchecked step, check the real state in the
code (do not trust the boxes alone), and propose the plan for that step: files to
create or touch, approach, and how we will verify the done.

Do NOT write code until I give you the OK. If the previous step has its done in
red, tell me before proposing anything.

When a step finishes with its done green: check the box in ROADMAP.md and commit.
If the step contains more than one intention, one commit per intention.

Before the closing commit: if the step took a design decision with a why,
deviated from the plan, hit friction in the harness, or measured something that
changed the criterion, add the entry to the ROADMAP log (format: what happened,
why it was resolved that way, the rule that remains or the signal to watch). If
the step was execution with none of that, do NOT add an entry: the log records
decisions and friction, not activity.
